const { createRefObj } = require("../utils/formatting-functions");
const { keyBy } = require("lodash");

describe("createRefObj", () => {
  test("", () => {});
});

[{},{},{}] key, 


const refobj = {
  Rachel: 1,
  bob: 2,
};

const createRef2 = (owners, value) => {}
